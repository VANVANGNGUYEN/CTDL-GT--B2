#include <stdio.h>

int giaiThua(int n) {
    if (n == 0) {
        return 1;
    } else {
        return n * giaiThua(n - 1);
    }
}

int main() {
    int n = 5;
    int ketQua = giaiThua(n);
    printf("%d! = %d\n", n, ketQua); // Output: 5! = 120

    return 0;
}
